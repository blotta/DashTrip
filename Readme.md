# DashTrip

Para rodar, abra o arquivo `DashTrip.sln` no Visual Studio e rode o projeto `AccountMicroservice`.

Importe o arquivo `Account.postman_collection.json` no Postman para testar o microserviço
